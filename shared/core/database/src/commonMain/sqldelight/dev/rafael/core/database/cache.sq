-- Cache genérico chave->JSON (tabela `kv_cache`): programas, treinos e stats.
-- Schema: 1.sqm. As chaves incluem o uid do dono.

put:
INSERT OR REPLACE INTO kv_cache(key, payload) VALUES (?, ?);

get:
SELECT payload FROM kv_cache WHERE key = ?;

deleteKey:
DELETE FROM kv_cache WHERE key = ?;
